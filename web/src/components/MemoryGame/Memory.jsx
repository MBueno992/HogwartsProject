import Card from './Card';

function Memory() {
  const [cards, setCards] = useState([cards]);

  //Las tarhetas tienen que tener id, status,
  return (
    <div>
      <h2>Memory</h2>
      <Card />
    </div>
  );
}

export default Memory;
